import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ABRIL-Ventas',
  webDir: 'dist',
  /*  server: {
    url: 'https://abril.arielbernardo.com/api',
    cleartext: true,
  }, */
};

export default config;
